{
  "private": true,
  "scripts": {
    "clean": "npm run clean --workspaces",
    "build": "tsc --build tsconfig.build.json && npm run build -w examples",
    "watch:tsc": "tsc --build --watch tsconfig.build.json",
    "watch": "concurrently -n sprotty,examples -c blue,green, \"npm run watch -w examples\" \"npm run watch:tsc\"",
    "lint": "eslint {**/src/**/*.ts,**/src/**/*.tsx}",
    "lint:fix": "eslint {**/src/**/*.ts,**/src/**/*.tsx} --fix",
    "test": "vitest run --config vite.config.mts",
    "coverage": "vitest run --coverage --config vite.config.mts"
  },
  "devDependencies": {
    "@types/node": "~24.0.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "@vitest/coverage-v8": "~4.1.0",
    "eslint": "^8.56.0",
    "concurrently": "~8.2.1",
    "eslint-config-prettier": "^9.0.0",
    "eslint-plugin-header": "^3.1.1",
    "eslint-plugin-no-null": "^1.0.2",
    "lerna": "^7.3.0",
    "reflect-metadata": "~0.1.14",
    "semver": "^7.5.4",
    "shx": "^0.3.4",
    "typescript": "~5.9.2",
    "vite": "~8.0.0",
    "vitest": "~4.1.0"
  },
  "resolutions": {
    "vite": "~8.0.0"
  },
  "engines": {
    "node": ">=24",
    "npm": ">=11.2.0"
  },
  "workspaces": [
    "packages/*",
    "examples"
  ]
}
