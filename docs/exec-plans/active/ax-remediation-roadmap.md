# Exec plan: AX remediation roadmap

- **Status**: active (in `docs/exec-plans/active/`; move the file to `completed/` when done)
- **Goal**: work off the gaps found in the 2026-08-24 agent-readiness audit, foundational first; done when every step below is either shipped or explicitly rejected.

Context: the audit set up AGENTS.md/CLAUDE.md, `docs/ARCHITECTURE.md`, ADR-0005, and fixed the broken command surface (per-package test scripts, Vitest 4 config, publish.yml branch filters, dead Jenkinsfile/Lerna, stale README commands and links, `.vscode` configs). Everything below was found but deliberately deferred.

## Decomposition

Foundational → sophisticated; each step is individually shippable.

- [ ] **Publish workflow follow-ups**: the `dry-run: ${{ github.event_name == 'pull_request' }}` input is vestigial (no `pull_request` trigger), and the Test step is advisory (`if: success() || failure()`) — decide whether tests should gate publishing.
- [ ] **Header rule promotion**: the interview confirmed the full EPL/GPL header block incl. `SPDX-License-Identifier` is mandatory, but `header/header` only checks the copyright line (a 2026 spec file already lacks the SPDX line). Extend the lint pattern to the full block, with a self-correction message pointing at a reference file.
- [ ] **Lint warning debt**: ~20 rules sit at `warn` and never gate (CI is green with warnings). Fix the 6 current warnings, then either promote the valuable rules to `error` + `--max-warnings 0` in CI, or drop the rest. Remove `eslint-plugin-no-null` (loaded, only rule set to `off`). Decide the prettier question: `eslint-config-prettier` is extended but prettier is not installed — adopt prettier or remove the config.
- [ ] **TypeScript strictness**: `strict: true` is not set; `strictFunctionTypes` and `strictPropertyInitialization` are silently off. Try enabling full `strict` and measure the fallout.
- [ ] **Structural dependency rules**: add dependency-cruiser (or similar) encoding the package layering (`sprotty-protocol` imports nothing; `sprotty-elk`'s core stays inversify-free) and cycle detection for package code — today the only cycle detector is webpack's plugin over the examples-reachable graph.
- [ ] **Test coverage gaps**: `features/edit` (9 source files, 0 specs), `expand`, `decoration`, `button`, `command-palette`, `fade`, `open`, `projection`, `undo-redo`, `ui-extensions`, and the new `pointer-tool.ts`/`touch-tool.ts` are untested; `sprotty-library` is published with zero tests and no `tsconfig.test.json`.
- [ ] **CHANGELOG backlog**: per-package changelogs stop at Dec 2024 (~20 months incl. the ESM and npm migrations unrecorded); root `CHANGELOG.md` index omits `sprotty-library`; `sprotty-library` has no CHANGELOG, README, or LICENSE file despite being published.
- [ ] **Examples cleanup**: dead `examples/custom-views-showcase/css/styles.css`; duplicate `id="sprotty-app"` in `multicore.html`; naming inconsistencies (`multicore/src/multicore.ts` vs the `standalone.ts` convention, two examples both named `random-graph.html`); normalize `configureModelElement(container, …)` to `context` in `random-graph*/src/di.config.ts`; `sprotty-library` lists `"css"` in package files but ships no css dir.
- [x] **Product specs — evidence-confirmed candidates** (2026-08-25 deep dive over the issue tracker, PR history, and downstream repos; each passes two evidence triggers — recurring bug-vs-intended threads *and* tier-1 downstream behaviour reliance; write on maintainer confirmation, strongest first):
  - a viewport spec — strongest dispute recurrence in the tracker: the fit-on-update cluster #121/#147/#164 open since 2019 (users ship sleep() workarounds), disputed zoom-limits default #441 (open), #302, #398, #220, #483; GLSP subclasses the viewport commands, all client repos dispatch Center/FitToScreen/SetViewport. The spec would adjudicate #441 and the #121 cluster.
  - an edge-routing spec — four threads adjudicated "intended" only inside comments: route preservation on node move (#284), the edge-containment contract (#193), anchors computed on demand not stored (#315), router vs. automatic-layout division (#298); plus the routing-handle view registration contract (#368) and open #452. All routers, three anchor computers, and the handle classes are subclassed in glsp-client; sprotty-vscode rebinds ManhattanEdgeRouter. Include the deliberately-opt-in status of the edge-intersection/edge-junction modules (maintainer decision 2026-08-25, see decision log).
  - a micro-layout spec — #106 and #165 each sat ~4 years in intended-vs-bug ambiguity; #27 (micro layout not re-run after macro layout) and #135 (compound bounds) are open, maintainer-acknowledged inconsistencies; Layouter/StatefulLayouter/HiddenBoundsUpdater are subclassed downstream and the sprotty.org micro-layout page already makes behaviour promises. Single-source note: the bounds *negotiation* stays in `docs/product-specs/client-server-protocol.md`; this spec holds the layout *semantics*.
- [x] **Client-server protocol spec amendments**: adjudicate the open intent items the mining surfaced — response forwarding for server-initiated requests (#445, maintainer intent stated in-thread), the websocket message-size limitation (#102, tolerated since 2019), the forwarded-action-kinds registration contract (#289), and `commitModel` semantics — the external model is replaced by a reduced copy on move/commit, and server re-layout erases manual positions unless the app persists them (#177, #306).
- [ ] **Deferred spec candidates, with evidence** — write spec-first when the area is next touched: selection (#94, #80 open since 2019, #128, #196, #307), model update/diffing/animation (#190, #452 open, #1), input bindings (#91 — behaviour changed, docs stale, filed as bug; #294, #194; spec-first with the v2.0 pointer-capture work #485/#488), edit (#29 open since 2018, #392 — where even maintainers had to guess original intent; precedes the edit test-gap work above). Recommended drop, maintainer to confirm: command-stack/undo-redo — the 2026-08-25 mining (searches over undo/redo/command-stack/history terms) found zero dispute threads, and glsp-client excludes the client-side undo-redo feature and ships its own (see decision log).
- [x] **ADR rescues** (decisions whose rationale exists only in threads): inversify version-alignment strategy — tried-and-reverted peerDependencies, Theia-aligned caret range (#292, #357, #429, #477); ESM-only migration, no dual build (#515, discussions #489/#508, #418); deprecation/breaking-change policy — deprecate in a minor with pointers, remove in one sweep at the next major (#355, #374, #231); request/response action pattern — JSON-RPC-like, requestId matching, RejectAction with serializable detail (#8, #85, #103, #184). Also: amend ADR-0002 with the "external model is the primary interface" statement from #355, marked as a dated addendum (decision unchanged).
- [x] **Design doc — edge routing architecture**: the one rescue too big for an ADR — args-based route pre-computation with the rejected IVNodePostprocessor alternative (#208/#226), IMultipleEdgesRouter for whole-graph routers (#246/#247), the 2019 registry redesign (#51), bezier support and its line-jump limitation (#245, #287). Opens a design-docs directory with an index and trust labels.
- [x] **ARCHITECTURE.md "why" paragraphs and gotchas** (cheap, high value): hidden-rendering rationale and the micro/macro layout split — recorded only in theia-ide/sprotty#48/#171/#180, a repo outside the org's control, so rescue-urgent; the performance principle behind the `ShapeView.isVisible` convention — DOM size is the bottleneck, thunks avoid re-patching, SVG export must not skip (#182); the JSX wrapper's purpose and the snabbdom `~3.5.1` pin whose reason (pure-ESM snabbdom vs. CJS sprotty, #418) lapsed with #515 — re-evaluate; the TouchTool/PointerTool parallel-tool design (#462/#475, #485/#488); implicit inter-module dependencies (#50/#127, open since 2019 — also a structural-sensor candidate); the projection wrapper div silently breaking sibling features (#299, #407, #302); ES2022 class-field emission breaking `'prop' in element` checks (found in #515 review).
- [x] **v2.0 exec plan**: collect the decisions parked in discussion #489 and milestone 12 — pointer-capture listener switch (#485), inversify 7, snabbdom unpin re-evaluation, SetModelCommand stays in the base module with `rebind` as the extension idiom.
- [ ] **Mechanized API surface**: add a committed, PR-diffed API report (e.g. api-extractor) for the published packages, regenerated in CI into a dedicated generated-docs directory, turning signature compatibility into a sensor; prose contracts then only carry what signatures can't.
- [ ] **Doc-freshness sensor**: declined in the 2026-08-24 session; revisit whether a mechanical link/command checker over AGENTS.md + docs/ should run in CI.
- [ ] **Governance visibility**: no CODEOWNERS, SECURITY.md, or issue/PR templates (Eclipse-delegated — decide if minimal in-repo ones are wanted); dependabot runs with no in-repo config (`.github/dependabot.yml` would make it legible); README wiki screenshots still point at the old `eclipse/sprotty` org path.
- [ ] **Website repo coordination** (sprotty-website): the core-components concepts page carries an unreviewed `<!-- AI GENERATED BELOW -->` section; tutorials pre-date the ESM migration and need re-verification; the TypeDoc API reference is regenerated manually (last 2025-09) — automate; the recipes never link their companion `examples/*-showcase/`; the landing-page showcase runs on a 2023 `0.14.0-next` prerelease of sprotty; consider publishing an `llms.txt` on sprotty.org for downstream consumer agents; and evaluate moving the docs content source into this repo with the site syncing from it (the Svelte model) so docs and code change in one reviewed PR — an org-level decision, proposal only.

## Progress log

- 2026-08-25 (renumbering): ADR indices rearranged to chronological decision order — safe because none were published yet (all on the unmerged agent-docs branch). Now: 0001 request/response (2019), 0002 protocol split (2021), 0003 deprecation policy (2023), 0004 inversify alignment (2024), 0005 OIDC publishing (2026, formerly 0001), 0006 ESM (2026). All cross-references updated; log entries below use the new numbers.
- 2026-08-25 (docs applied): the maintainer confirmed the recommendations; shipped: three product specs (`viewport.md`, `edge-routing.md`, `micro-layout.md`) + protocol-spec amendments + index rows; the four rescued ADRs (now 0001/0003/0004/0006) + the ADR-0002 addendum; `docs/design-docs/` opened with the edge-routing design doc and a trust-labeled index; the ARCHITECTURE.md why-paragraphs and gotchas; `docs/exec-plans/active/v2-release.md`; an AGENTS.md pointer to design-docs. check_docs pass and full build/lint/test run captured in-session.
- 2026-08-25 (evidence deep-dive): Issue tracker, PR history, and downstream repos mined by three research passes — bug-vs-intended dispute recurrence per capability (all cited threads read), design-decision rescue candidates, and downstream behaviour reliance (glsp-client 1.4.0 pin, sprotty-vscode, sprotty-theia 0.12, langium-sprotty; subclass/rebind weighted over imports). The docs-layer roadmap items above were rewritten with the evidence attached; ~17 verbatim maintainer intent statements were identified in threads as direct spec source material (retrievable via the cited issue numbers with `gh`).
- 2026-08-25: Feature-module index added to `docs/ARCHITECTURE.md`; `docs/product-specs/` opened with the client-server protocol spec; ADR-0002 rescued; AGENTS.md gained the behaviour-contract coupling.
- 2026-08-24: Plan created from the agent-readiness audit (fleet of 5 research agents + verification by execution). Nothing below the fold started.

## Decision log

- 2026-08-25 (maintainer): `edgeIntersectionModule`/`edgeJunctionModule` stay opt-in — a deliberate maintainer decision (now recorded in the edge-routing spec and design doc; resolves the open question below). The micro/macro layout rationale was stated for the record: graph layout is an academic, mostly NP-hard domain delegated to engines (ELK lineage: Kiel University, ~2008); the engine/client boundary is configurable but complicated at node sizes, port positions, and label positions — label layout is a per-application choice between engine and micro-layout (recorded in ARCHITECTURE.md and the micro-layout spec).
- 2026-08-25 (evidence deep-dive, pending maintainer confirmation): spec shortlist revised on tracker + downstream evidence — viewport, edge routing, and micro-layout promoted to confirmed candidates (each has recurring dispute threads *and* tier-1 downstream reliance); command-stack/undo-redo recommended for drop (targeted searches found zero dispute threads; glsp-client explicitly excludes the client-side undo-redo module and ships its own — reliance is on the protocol Undo/RedoAction only, which falls under the protocol spec's area); edit retained as a deferred spec-first candidate (borderline: #29, #392).
- 2026-08-25: Spec/design-doc triage with the maintainer — bug-vs-intended has no durable in-repo adjudicator (trigger confirmed); one spec created (`docs/product-specs/client-server-protocol.md`); sprotty.org-documented behaviour confirmed as a compatibility promise (recorded in AGENTS.md); design-history rescue limited to key PR/issue threads (ADR-0002 rescued from #233/#243); edit + command-stack specs deferred.
- 2026-08-24: Doc-freshness CI check (check_docs.py) declined by the maintainer for now — kept as a roadmap item instead.
- 2026-08-24: Vitest single-worker pin removed (suite proven green with 8 workers); per-package `test` scripts repaired; publish.yml branch filters fixed to `master`/`maintenance-*`; Jenkinsfile and Lerna removed (see ADR-0005).

## Open questions

- Should the publish workflow's Test step gate the publish?
- Confirm the recommended drop of the command-stack/undo-redo spec candidate (see decision log).
